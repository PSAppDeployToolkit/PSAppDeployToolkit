<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Fluence.Wpf.Controls">

    <!--  Eye glyph for reveal button (Segoe Fluent Icons: RedEye = E7B3)  -->
    <StreamGeometry x:Key="EyeGlyph">M 12,4.5 C 7,4.5 2.73,7.61 1,12 C 2.73,16.39 7,19.5 12,19.5 C 17,19.5 21.27,16.39 23,12 C 21.27,7.61 17,4.5 12,4.5 Z M 12,17 C 9.24,17 7,14.76 7,12 C 7,9.24 9.24,7 12,7 C 14.76,7 17,9.24 17,12 C 17,14.76 14.76,17 12,17 Z M 12,9 C 10.34,9 9,10.34 9,12 C 9,13.66 10.34,15 12,15 C 13.66,15 15,13.66 15,12 C 15,10.34 13.66,9 12,9 Z</StreamGeometry>

    <!--
        System.Windows.Controls.PasswordBox is sealed, so this style dresses the native control instead of
        a derived type. Everything a derived type would expose as a dependency property lives on
        controls:PasswordBoxExtensions as an attached property, and the IsFluentDecorated setter below
        attaches the reveal, Caps Lock, and strength behavior.
    -->
    <Style TargetType="{x:Type PasswordBox}">
        <Setter Property="controls:PasswordBoxExtensions.IsFluentDecorated" Value="True" />
        <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TextControlElevationBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CaretBrush" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="FontFamily" Value="{DynamicResource FluentFontFamily}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Padding" Value="10,5,6,6" />
        <Setter Property="SelectionBrush" Value="{DynamicResource AccentFillColorDefaultBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type PasswordBox}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border
                            x:Name="MainBorder"
                            Grid.Row="0"
                            MinHeight="{TemplateBinding MinHeight}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Grid Grid.Column="0">
                                    <TextBlock
                                        x:Name="PlaceholderTextBlock"
                                        Margin="{TemplateBinding BorderThickness}"
                                        Padding="{TemplateBinding Padding}"
                                        VerticalAlignment="Center"
                                        FontFamily="{TemplateBinding FontFamily}"
                                        FontSize="{TemplateBinding FontSize}"
                                        Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                        IsHitTestVisible="False"
                                        Visibility="Collapsed" />

                                    <!--  The native secure text view. Peeking fades this to zero opacity rather than collapsing it, so the caret, selection, and hit testing stay live behind the overlay.  -->
                                    <ScrollViewer
                                        x:Name="PART_ContentHost"
                                        Margin="{TemplateBinding BorderThickness}"
                                        Padding="{TemplateBinding Padding}"
                                        VerticalAlignment="Center"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Hidden" />

                                    <!--  Peek overlay: never focusable, never a tab stop, and emptied the moment the peek ends.  -->
                                    <TextBox
                                        x:Name="PART_RevealDisplay"
                                        Margin="{TemplateBinding BorderThickness}"
                                        Padding="{TemplateBinding Padding}"
                                        VerticalAlignment="Center"
                                        VerticalContentAlignment="Center"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        FontFamily="{TemplateBinding FontFamily}"
                                        FontSize="{TemplateBinding FontSize}"
                                        Focusable="False"
                                        Foreground="{TemplateBinding Foreground}"
                                        IsHitTestVisible="False"
                                        IsReadOnly="True"
                                        IsTabStop="False"
                                        Visibility="Collapsed" />
                                </Grid>

                                <Button
                                    x:Name="PART_RevealButton"
                                    Grid.Column="1"
                                    Width="30"
                                    Margin="0"
                                    Padding="0"
                                    AutomationProperties.Name="Show password"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Cursor="Arrow"
                                    Focusable="True"
                                    Visibility="Collapsed">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid
                                                x:Name="ButtonLayoutGrid"
                                                Margin="0,4,4,4"
                                                Background="Transparent">
                                                <Border
                                                    x:Name="ButtonBackground"
                                                    Background="Transparent"
                                                    CornerRadius="{DynamicResource ControlCornerRadius}" />
                                                <Path
                                                    x:Name="GlyphPath"
                                                    Width="16"
                                                    Height="16"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Data="{StaticResource EyeGlyph}"
                                                    Fill="{DynamicResource TextFillColorSecondaryBrush}"
                                                    Stretch="Uniform" />
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="ButtonBackground" Property="Background" Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter TargetName="ButtonBackground" Property="Background" Value="{DynamicResource SubtleFillColorTertiaryBrush}" />
                                                    <Setter TargetName="GlyphPath" Property="Fill" Value="{DynamicResource TextFillColorTertiaryBrush}" />
                                                </Trigger>
                                                <Trigger Property="IsEnabled" Value="False">
                                                    <Setter TargetName="ButtonLayoutGrid" Property="Opacity" Value="0" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                        </Border>

                        <Border
                            x:Name="FocusAccentLine"
                            Grid.Row="0"
                            Height="2"
                            VerticalAlignment="Bottom"
                            Background="{DynamicResource AccentFillColorDefaultBrush}"
                            CornerRadius="0,0,4,4"
                            Opacity="0" />

                        <StackPanel Grid.Row="1" Margin="12,6,12,0">
                            <TextBlock
                                x:Name="PART_CapsLockIndicator"
                                FontFamily="{TemplateBinding FontFamily}"
                                FontSize="12"
                                Foreground="{DynamicResource SystemFillColorCautionBrush}"
                                Text="Caps Lock is on"
                                Visibility="Collapsed" />

                            <StackPanel
                                x:Name="PART_StrengthMeter"
                                Margin="0,4,0,0"
                                Orientation="Horizontal">
                                <Border
                                    x:Name="PART_StrengthSegment0"
                                    Width="40"
                                    Height="4"
                                    Margin="0,0,4,0"
                                    Background="{DynamicResource ControlFillColorTertiaryBrush}"
                                    CornerRadius="2"
                                    Opacity="0.25" />
                                <Border
                                    x:Name="PART_StrengthSegment1"
                                    Width="40"
                                    Height="4"
                                    Margin="0,0,4,0"
                                    Background="{DynamicResource ControlFillColorTertiaryBrush}"
                                    CornerRadius="2"
                                    Opacity="0.25" />
                                <Border
                                    x:Name="PART_StrengthSegment2"
                                    Width="40"
                                    Height="4"
                                    Margin="0,0,4,0"
                                    Background="{DynamicResource ControlFillColorTertiaryBrush}"
                                    CornerRadius="2"
                                    Opacity="0.25" />
                                <Border
                                    x:Name="PART_StrengthSegment3"
                                    Width="40"
                                    Height="4"
                                    Background="{DynamicResource ControlFillColorTertiaryBrush}"
                                    CornerRadius="2"
                                    Opacity="0.25" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!--
                            PasswordBoxExtensions writes the placeholder, the corner radius, the reveal button
                            visibility, and the peek swap from code rather than from triggers. Password is not a
                            dependency property on the native control, and a prefixed attached-property binding
                            path ("(controls:PasswordBoxExtensions.HasPassword)") does not resolve at runtime
                            from a BAML theme dictionary.
                        -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                        </Trigger>

                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource ControlFillColorInputActiveBrush}" />
                            <Setter TargetName="FocusAccentLine" Property="Opacity" Value="1" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="MainBorder" Property="Background" Value="{DynamicResource ControlFillColorDisabledBrush}" />
                            <Setter TargetName="MainBorder" Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource TextFillColorDisabledBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="UseLayoutRounding" Value="True" />
    </Style>

</ResourceDictionary>
