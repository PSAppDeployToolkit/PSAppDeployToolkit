<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Fluence.Wpf.Controls"
    xmlns:ui="clr-namespace:Fluence.Wpf">

    <Style TargetType="{x:Type controls:ProgressRing}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Foreground" Value="{DynamicResource AccentFillColorDefaultBrush}" />
        <Setter Property="Height" Value="32" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="MinHeight" Value="20" />
        <Setter Property="MinWidth" Value="20" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ProgressRing}">
                    <Grid x:Name="LayoutRoot" Background="{TemplateBinding Background}">

                        <!--
                            Indeterminate caterpillar arc.  Geometry is set from code-behind
                            as the animated start/sweep angles change; the path keeps the
                            same Fluent accent foreground and rounded caps as determinate mode.
                        -->
                        <Path
                            x:Name="PART_IndeterminateArc"
                            Stroke="{TemplateBinding Foreground}"
                            StrokeEndLineCap="Round"
                            StrokeStartLineCap="Round"
                            StrokeThickness="{TemplateBinding StrokeThickness}"
                            Visibility="Collapsed" />

                        <!--
                            Determinate arc.  Path geometry is set from code-behind
                            (ProgressRing.RenderDeterminateArc) whenever Value, Minimum,
                            Maximum, or StrokeThickness changes.  A 150 ms ease-in-out
                            animation on a private AnimatedFraction DP drives the tween.
                        -->
                        <Path
                            x:Name="PART_DeterminateArc"
                            Stroke="{TemplateBinding Foreground}"
                            StrokeEndLineCap="Round"
                            StrokeStartLineCap="Round"
                            StrokeThickness="{TemplateBinding StrokeThickness}"
                            Visibility="Collapsed" />
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!--  Active + Indeterminate: show the code-driven caterpillar arc.  -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsActive" Value="True" />
                                <Condition Property="IsIndeterminate" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_IndeterminateArc" Property="Visibility" Value="Visible" />
                        </MultiTrigger>

                        <!--  Active + Determinate: show arc Path.  Geometry is driven by code-behind.  -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsActive" Value="True" />
                                <Condition Property="IsIndeterminate" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_DeterminateArc" Property="Visibility" Value="Visible" />
                        </MultiTrigger>

                        <Trigger Property="ProgressState" Value="{x:Static ui:ProgressRingState.Paused}">
                            <Setter TargetName="PART_IndeterminateArc" Property="Stroke" Value="{DynamicResource SystemFillColorCautionBrush}" />
                            <Setter TargetName="PART_DeterminateArc" Property="Stroke" Value="{DynamicResource SystemFillColorCautionBrush}" />
                        </Trigger>

                        <Trigger Property="ProgressState" Value="{x:Static ui:ProgressRingState.Error}">
                            <Setter TargetName="PART_IndeterminateArc" Property="Stroke" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                            <Setter TargetName="PART_DeterminateArc" Property="Stroke" Value="{DynamicResource SystemFillColorCriticalBrush}" />
                        </Trigger>

                        <!--  Disabled: dim everything.  -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="LayoutRoot" Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Width" Value="32" />
    </Style>

</ResourceDictionary>
